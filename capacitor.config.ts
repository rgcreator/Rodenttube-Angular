import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fcknaagdier.rodenttubeangular',
  appName: 'rodenttube',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
